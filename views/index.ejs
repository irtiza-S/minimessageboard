<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% for(var message of messages) { %>
      <li><%= message.user %></li>
      <li><%= message.text %></li>
      <li><%= message.added %></li>
    <% } %>

    <a href="/new"><h2>Add a new message</h2></a>
  </body>
</html>

<!-- finished -->